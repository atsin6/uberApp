spring.application.name=uberApp

#DB configurations
spring.datasource.url=jdbc:postgresql://${DB_HOST_URL}:5432/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}


spring.jpa.hibernate.ddl-auto=update


#distanceService.base.url = https://router.project-osrm.org/route/v1/driving/

jwt.secretKey = ${JWT_SECRETKEY}

#smtp config
spring.mail.username=atulsin639@gmail.com
spring.mail.password=fhry zoby rlom zwjo
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true